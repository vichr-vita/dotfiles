## 1. Module and prompt

- [x] 1.1 Add `vichr.ai_edit` setup defaults, option validation, eligible-buffer checks, and authoritative normal/visual target capture covering inclusive/exclusive, forward/reverse, single/multiline, ASCII, and multibyte characterwise ranges plus linewise ranges.
- [x] 1.2 Implement the centered scratch prompt with file/range title, Enter submission, `<C-j>` newline insertion, Escape cancellation, and empty-instruction validation.
- [x] 1.3 Register normal/visual `<leader>ai` mappings and the buffer-aware `:AIEditCancel` command from the existing Neovim configuration.

## 2. Staging and OpenCode execution

- [x] 2.1 Implement project-root resolution, private per-run staging directories, whole-buffer or selection snapshots, read-only full-buffer context, extension preservation, and idempotent cleanup.
- [x] 2.2 Add the trusted `stage_text` OpenCode tool with fixed target/read-only-context source enums, measured single-line page envelopes below the fixed output budget, sequential EOF coverage tracking for multi-page whole replacement, exact-match submissions, overlap/count/stale-revision checks, path/type/size validation, and same-directory atomic target replacement including empty output.
- [x] 2.3 Bootstrap trusted helper source and matching `@opencode-ai/plugin` dependency in unique directories, verify and seal each complete build, atomically publish one immutable content-addressed winner under concurrency, and report bootstrap and offline failures.
- [x] 2.4 Generate a unique runtime primary agent with inherited resolved global model/provider settings and unchanged XDG OAuth data plus the complete bundled provider/auth plugin set shipped inside exact OpenCode 1.18.21; isolate real global, legacy-home, and project custom-tool directories while keeping configured plugins empty and disabling sharing, snapshots, formatters, LSP, stock mutation tools, shell, and fallback mutation; enable only read, glob, grep, and `stage_text` for the model.
- [x] 2.5 Before any registry-loading command, resolve and validate global config through `debug config`; then preflight exact OpenCode version 1.18.21, isolated final resolved config, and isolated agent tools, aborting before registry loading or session creation unless every safety value, empty configured-plugin and MCP lists, 32 KiB/10-line tool-output limit, and enabled tool matches the expected set while the inseparable bundled provider/auth plugins remain available.
- [x] 2.6 Launch `opencode run` asynchronously with argv-safe `vim.system`, prompt input through stdin, streamed JSON/session/tool-state capture, one-job-per-buffer tracking, configurable timeout, process cancellation, and race-safe finalization.

## 3. Result handling

- [x] 3.1 Require zero exit, exactly one successful `stage_text` submit, no top-level or nested tool errors, a valid staged target path, target buffer identity and eligibility, and the captured changed tick before application.
- [x] 3.2 Apply whole-buffer and selection results with one Neovim buffer API call, preserve unsaved state and end-of-line behavior, and handle valid deletion and byte-identical no-op results.
- [x] 3.3 Add running, success, no-op, stale, cancellation, timeout, dependency, and session-cleanup notifications plus a readonly scratch float for captured run errors.
- [x] 3.4 Disable automatic sharing and snapshots, then attempt asynchronous bounded-time deletion of every observed OpenCode session after success, failure, cancellation, or timeout without delaying buffer results, staging cleanup, or editor timers.

## 4. Verification

- [x] 4.1 Add direct tests for target and read-only-context pagination past 50 KiB and 2,000 lines, serialized envelope byte/line limits, source/revision/cursor handling, EOF coverage before multi-page replacement, exact replacements without full reads, context-submit rejection, count and overlap rejection, containment, symlink and file-identity rejection, output limit, empty replacement, and inability to choose another path.
- [x] 4.2 Add a minimal headless Neovim test harness with a fake OpenCode executable and cover normal-buffer, direct inclusive/exclusive forward/reverse single/multiline ASCII/multibyte characterwise and linewise selection staging, cache bootstrap, unique dynamic agent setup, exact-version/config/tool preflight, trusted bundled defaults, disabled configured/project/external plugins, disabled sharing/snapshots/formatters/LSP/stock writes, stdin prompts beginning with flags, and strict-scope application.
- [x] 4.3 Cover unsupported buffers and block selections, size limits, empty prompts, concurrent jobs, stale buffers, missing executable, no-op, nonzero/top-level-error/nested-tool-error/missing-or-duplicate-submission runs, cancellation, timeout, delayed and never-exiting asynchronous session cleanup with independent timer progress and prompt staging removal, and one-step undo.
- [x] 4.4 Run Stylua, trusted-tool checks, headless tests, and a clean Neovim startup smoke test.
- [x] 4.5 Run whole-buffer and visual-selection smoke edits through installed OpenCode 1.18.21 with real OpenAI OAuth when credentials and network access are available, and confirm review, undo, manual-save behavior, and session cleanup.
- [x] 4.6 Verify with actual installed OpenCode 1.18.21 plus fake-only failure cases against a disposable fixture containing executable project OpenCode extensions, a managed local-MCP process sentinel, managed unsafe and tiny-output-limit overrides, automatic sharing, a colliding/disabled agent, stock `apply_patch` move attempts, formatter/LSP sentinel commands, large snapshots with tail markers, and an in-project external symlink; self-bootstrap the exact helper dependency from an empty isolated XDG cache, then confirm exact executable/config/tool preflights, trusted bundled defaults, empty configured plugins and MCP configuration, managed MCP rejection before process execution or model-session creation, project/external extensions and host commands never execute, real `apply_patch` is denied or omitted, writes stay staged, the project target remains byte-identical, full target content remains intact, sharing stays off, and the symlink risk matches the documented trusted-worktree boundary.
